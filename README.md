# HID_Joystick

Visual Micro Arduino sketch that is intended to present a USB HID joystick on a capable board. The `.ino` still contains empty `setup()` and `loop()` bodies from the Visual Micro template, so it is a starting point rather than a finished device. Kept for Dave Robinson / VaderConsulting as a historical working copy from the Arduino archive.

**Source last updated:** 2022-05-08  
**Language:** C++ / Arduino  
**Target:** Arduino (Visual Micro / Visual Studio, Win32/x64 toolchain metadata)  
**Output:** Arduino sketch (`.ino`) plus Visual Micro `.vcxproj`

## Solution structure

| Project | Language | Type | Purpose |
|---------|----------|------|---------|
| `HID_Joystick` | C++ / Arduino | sketch | USB HID joystick stub (empty setup/loop) |
| `HID_Joystick.vcxproj` | C++ | Visual Micro project | Opens the sketch inside Visual Studio |

## How to open

Open `HID_Joystick.ino` in the Arduino IDE, or open `HID_Joystick.vcxproj` in Visual Studio with Visual Micro.

## Attribution and provenance

Author comments name `STUDIO\windo` (8 May 2022). Imported from Dave Robinson's Arduino archive. No third-party library tree is bundled in this repository.

## License

MIT © 2026 VaderConsulting for Dave's code. See `LICENSE`.
